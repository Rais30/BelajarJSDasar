function ulang() {
    var nama = prompt("masukan nama siswa: ")
    var nilai1 = parseInt(prompt("masukan nilai ujian pertama: "))
    var nilai2 = parseInt(prompt("masukan nilai uian ke 2: "))
    var nilai3 = parseInt(prompt("masukan nilai ujian ke tiga: "))
    var nilairata = (nilai1 + nilai2 + nilai3) / 3

    var namasiswa = [{
            kelas1: "yoga",
            "yamin",
            "fathul",
        },
        {
            kelas2: ""
        }
    ]
}